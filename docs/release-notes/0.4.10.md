# Omarchy News Radar 0.4.10

Light-theme contrast and theme-tinted icon badge:

- Remove the opaque `#111` squircle from the Apps/panel SVG; keep the green Omarchy glyph and amber radar geometry on a transparent background.
- In the panel header, draw an opaque rounded `Color.popups.background` badge behind the Image so Lupine/light and Matte Black/dark both tint the mark.
- Raise secondary/quiet text floors on light popup backgrounds; keep dark near the prior Matte-Black-tuned alphas.
- Replace modal scrims (`Color.background` @ 0.82) with foreground dimming so light themes no longer wash out.
- Apps-menu uses the transparent SVG as best-effort; it cannot apply QML theme fills.

No feed or local-state schema bump. Community plugin; not an official Omarchy project.
