# Omarchy News Radar 0.4.5

Icon refresh only:

- Replace the plugin SVG with an ASCII-art radar face whose masthead reads **== Omarchy ==**.
- Apps-menu / launcher icon follows the same asset via the existing manifest `icon` path.

No feed or local-state schema bump. Panel layout and keyboard bindings are unchanged.
