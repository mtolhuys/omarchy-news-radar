# Omarchy News Radar 0.4.11

Panel header icon chip contrast on light themes (Lupine):

- Keep the opaque `Color.popups.background` rounded plate behind the mark.
- Draw a visible `Color.popups.border` so the squircle reads as a chip against the header (not a washed same-color plate).
- On light popup backgrounds (luminance > ~0.5), load `assets/io.github.mtolhuys.news-radar-light.svg` with Omarchy green glyph opacity ~0.80 and slightly stronger amber.
- Dark / Matte Black keep the existing transparent SVG; border remains subtle.
- Apps-menu / XDG launcher still use the manifest dark-tuned transparent SVG.

No feed or local-state schema bump. Community plugin; not an official Omarchy project.
