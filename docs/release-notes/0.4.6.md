# Omarchy News Radar 0.4.6

Icon contrast fix only:

- Brighten the ASCII Omarchy radar SVG fills: masthead `#c8c8c8`, paper `#9a9a9a`, amber `#e0a04a` (was invisibly dim in 0.4.5).
- Masthead text remains **== Omarchy ==**; Apps-menu / launcher icon follows the same asset.

No feed or local-state schema bump. Panel layout and keyboard bindings are unchanged.
