# Omarchy News Radar 0.4.4

Layout polish on the 0.4.3 Keys-under-SECTIONS footing:

- Quieter **Keys** legend (unboxed key·action pairs, soft toggle, lower-contrast captions).
- Wider detail inspector (~36%) with a slightly slimmer story list (~45%) and a lightly trimmed SECTIONS rail (~14%).

No feed or local-state schema bump. Keyboard `a` / `f` / `?` behavior is unchanged.
