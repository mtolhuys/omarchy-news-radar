# Omarchy News Radar 0.4.13

## Highlights

- Official Omarchy News now lands in Core beside releases via the allowlisted RSS feed at `https://omarchy.org/news/rss.xml`.
- Clients still only fetch the published `events.json`; no HTML scraping and no API key.
- Front Page keeps a calm edition: news stays routine and at most three recent Omarchy News items are admitted by quota (D048).

## Notes

- Feed schema remains v2 and local state remains v10; enums gain `omarchy-news` / `omarchy-news` source id.
- Collection fails closed and retains the prior news snapshot when the RSS fetch or validation fails.
- CI uses checked-in RSS fixtures only.
