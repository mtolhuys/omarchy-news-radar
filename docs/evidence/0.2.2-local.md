# Version 0.2.2 local-candidate evidence

## Candidate identity

- Local runtime candidate: `25ae19eb0b9184372b359b934ef22ea98bbafe2a`.
- Manifest and Python package version: `0.2.2`; panel build identity: `news-radar-0.2.2+identity-1`; helper protocol: `1`.
- Omarchy source: commit `83881e979b35468c3e7d60b171e319ede61a88fd`.
- Plugin Lab base: `omarchy-2026.08.27-x86_64-local`.
- Full acceptance evidence: run `20260902-164914`; its disposable overlay was discarded.

## Root cause and repair

The bar previously counted every unread event in the validated feed, while the newspaper projected stories through persistent per-section filters. With **With images** enabled for Plugins, unread image-less plugin stories could remain in the raw bar count even when no section exposed them. The badge therefore advertised unread work that the current newspaper could not reveal.

The badge now counts the deduplicated union of unread event IDs surviving the persistent filters for Front Page, For You, Core, Plugins, and Saved. The bar loads the same exact enabled-plugin IDs used by the panel so Front Page and For You remain consistent. Search text and pagination stay transient and do not alter the badge.

## Verification

- `make test`: passed all 106 offline unit and integration tests.
- The focused regression reproduces the reported image-filter mismatch, verifies zero when all routes hide those unread stories, verifies installed-plugin-aware For You projection, and verifies that resetting Plugins makes the stories count again.
- The QML contract verifies that the bar loads enabled plugin IDs before requesting its first indicator model and supplies them on every indicator request.
- `OMARCHY_SOURCE=/home/mtolhuijs/Projects/omarchy/omarchy make validate`: passed Python, repository, JSON/schema/feed/manifest/workflow, ShellCheck, desktop-entry, and QML validation.
- Full exact-version disposable Plugin Lab acceptance passed run `20260902-164914` with 48 screenshots. It covered bar unread propagation, rendered per-section filters and reset, unread-only behavior, installation, Apps-menu and shortcut activation, window management, keyboard and pointer navigation, themes, same-path runtime replacement, disable/re-enable, and clean removal.
- An earlier run, `20260902-164620`, reached the normal window-management phase before the disposable guest's idle lock screen intercepted its AltTab assertion. Its failure screenshot records the lock screen; no News Radar or QML error was present. The clean rerun passed that assertion and the rest of the suite.

No host desktop configuration or live session was changed. No commit, tag, release, or marketplace issue was pushed or updated.
