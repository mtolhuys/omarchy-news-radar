# Version 0.2.2 local-candidate evidence

## Candidate identity

- Local runtime candidate: `3904270f3b4bf050a41eb5078a7e94cbfd37dc23`; subsequent test/evidence-only edits do not change executable behavior.
- Manifest and Python package version: `0.2.2`; panel build identity: `news-radar-0.2.2+identity-1`; helper protocol: `1`.
- Omarchy source: commit `83881e979b35468c3e7d60b171e319ede61a88fd` from the selected checkout.
- Plugin Lab base: `omarchy-2026.08.27-x86_64-local`.
- Full acceptance evidence: run `20260902-171640`; its disposable overlay was discarded.

## Root cause and repair

The bar previously counted every unread event in the validated feed, while the newspaper projected stories through persistent per-section filters. With **With images** enabled for Plugins, unread image-less plugin stories could remain in the raw bar count even when no section exposed them. The badge therefore advertised unread work that the current newspaper could not reveal.

The badge now counts the deduplicated union of unread event IDs surviving the persistent filters for Front Page, For You, Core, Plugins, and Saved. The bar reloads the same exact enabled-plugin IDs used by the panel before every coalesced indicator request, so Front Page and For You do not retain a lifetime-stale enablement snapshot. Search text and pagination stay transient and do not alter the badge.

## Verification

- `make test`: passed all 108 offline unit and integration tests.
- The focused regression reproduces the reported image-filter mismatch, derives the expected value from the public projections, verifies overlap deduplication and Saved reachability, verifies installed-plugin-aware For You projection, and verifies that resetting Plugins makes the stories count again.
- The QML contract verifies that the bar reloads enabled plugin IDs for coalesced indicator work and retries an update queued while the helper is active.
- `OMARCHY_SOURCE=<selected-omarchy-checkout> make validate`: passed Python, repository, JSON/schema/feed/manifest/workflow, ShellCheck, desktop-entry, and QML validation.
- `mypy 2.3.1 --check-untyped-defs radar scripts tests`: no issues in 55 source files.
- `ruff 0.16.5 check --select F,E9 radar scripts tests`: passed. Its unconfigured formatter would rewrite 44 pre-existing files, so this focused change does not introduce repository-wide formatting churn.
- `bandit 1.9.4 -q -r -ll radar scripts`: no medium/high security finding.
- `vulture 2.16 radar scripts tests/lab/prepare_fixtures.py --min-confidence 90`: no dead code at the selected confidence.
- `check-jsonschema 0.38.0 --check-metaschema schemas/*.json`: passed.
- `coverage.py 7.16.0 run --branch -m unittest discover -s tests -p 'test_*.py'`: 108 tests passed with 84 percent total branch coverage; the affected `radar/client.py`, `radar/filters.py`, and `radar/model.py` modules measured 84, 85, and 80 percent respectively.
- `detect-secrets 1.5.0 scan <tracked-files>` reported only the intentional rejected basic-auth URL fixture and two historical evidence references to that fixture; review found no credential material.
- A synthetic maximum-size 500-event indicator benchmark completed 100 runs at a 101.250 ms mean on the development host. The guest's existing 120-story projection checkpoint completed in 0.225 seconds, and no helper remained after close.
- Full exact-version disposable Plugin Lab acceptance passed run `20260902-171640` with 48 reviewed screenshots. It covered repeated closed-panel badge propagation, helper cleanup, rendered per-section filters and reset, installed-plugin For You matching, Saved, unread-only behavior, installation, Apps-menu and shortcut activation, window management, keyboard and pointer navigation, themes, same-path runtime replacement, disable/re-enable, and clean removal.
- Log review found no QML load, type, reference, or product failure. The fixture server recorded one expected connection reset when a bounded negative-path client deliberately abandoned a response.

No host desktop configuration or live session was changed. No commit, tag, release, or marketplace issue was pushed or updated.
