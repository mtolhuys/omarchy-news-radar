# 0.5.4 local candidate evidence

Status: source, disposable-desktop, and owner daily-desktop gates passed. The owner approved publication on 2026-09-07. Remote CI, exact public-clone installation, and the in-place marketplace verification update remain publication gates.

## Regression coverage

- `make validate` passed Python compilation, repository contracts, JSON and schema validation, ShellCheck for 15 scripts, and desktop entry validation. Host `qmllint` remained unavailable because `OMARCHY_SOURCE` is not configured there.
- `make test` passed all 329 tests. The new regressions prove that the generic setup-news companion survives a marketplace flood beyond the 500-event rolling feed, stays source-backed and bounded, preserves the last valid cache on failure, imports only when its digest matches, and restores exact enabled-plugin matches without broadening For You.
- The source build produced a current 2,528-record companion within its 8 MiB transport bound. The candidate installed on the owner's desktop reported 500 rolling-feed events, 414 validated images, and all 2,528 source-backed setup-news records.
- A copied real-state regression marked all 284 matching visible Plugins stories read and then reported zero unread among the complete 2,320-story local projection, without modifying the owner's real state.
- Repeated complete projection improved from roughly 1.16 seconds to 0.67 seconds on the owner's current data. Startup now performs one initial projection and skips a second projection when the digest-bound companion is unchanged.

## Disposable Omarchy acceptance

Plugin Lab run `20260907-154200` passed from a clean base-image overlay. It installed the exact candidate checkout, imported a pictured rolling edition and its digest-bound setup-news companion, and proved that a companion-only plugin absent from the rolling feed appeared in For You only for its exact installed ID. The same run passed Apps entry and icon installation, default-on presentation, bar geometry, local image resolution, clean fast-forward updates, idempotence, dirty-source refusal, legacy panel migration, and complete removal.

The Plugin Lab retained the run's seven screenshots and logs under its normal run-artifact directory for local review. A final clean-candidate acceptance run is recorded below before publication.

## Owner acceptance

The owner tested runtime commit `492a797b867bea304a2a609ec5a68815687ec3b9` on the daily desktop. Plugins showed the complete marketplace timeline, For You stayed limited to locally relevant enabled plugins and explicit follows, and filtered **Mark all as read** worked. The owner explicitly approved publication. Subsequent release-preparation changes are documentation and the public-install version assertion only; they do not alter runtime behavior.

## Remaining publication gates

- Run full disposable acceptance from the final clean local commit and retain its run ID.
- Push that one commit, require green remote CI, then prove a clean public clone resolves its exact 40-character SHA.
- Update the existing marketplace verification issue in place. Do not create another issue or comment.
