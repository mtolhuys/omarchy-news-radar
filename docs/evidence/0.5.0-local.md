# Version 0.5.0 local-candidate evidence

## Candidate identity

- Runtime candidate: `fce50ffa5aaf7f2ee995cfe7c719099fa004771d` on `feat/finite-briefing`, with a clean source tree during both final scenario staging operations. The subsequent documentation-only commit records these results and does not change executable behavior.
- Manifest and Python package: `0.5.0`; panel: `news-radar-0.5.0+identity-1`; helper build: `news-radar-0.5.0`; helper protocol: `1`.
- Feed schema: `2`; local state schema: `12`.
- Published release remains `0.4.16`. This evidence establishes a local candidate, not publication or marketplace promotion.

## Source and publication artifacts

- `make test`: all 201 offline unit and integration tests passed. Coverage includes deterministic bounded selection, exact group and briefing read scope, unfinished-brief advancement, state migration, welcome behavior, concurrent feed adoption, stale-action rejection, gzip limits and corruption, cache preservation, publication escaping, and candidate documentation.
- `OMARCHY_SOURCE=<selected-checkout> make validate`: passed Python compilation, tracked text, JSON/schema/feed/manifest/workflow checks, generated-file drift, ShellCheck for 13 scripts, desktop-entry validation, and QML lint against the selected Omarchy source.
- `make feed-fixture` and `make site`: passed. Fixture edition digest: `dbdf9c8a8a3ed8dd2f0561294dc3c63a1b5ef6be68912ba876723cb59c979ec7`. Generated HTML SHA-256: `f23006c22908604c7ac3a53d829a1acf3ae1dfd3b936fa49ce0d3f2406d07194`.
- Browser review covered the generated fixture web edition in wide light/dark layouts and at a 390-pixel width, including 200-percent text. The install and RSS links, human-readable dates, wrapping, and absence of horizontal overflow were checked. This was a local preview of the existing Python publisher output; no Forge deployment occurred.

## Disposable desktop evidence

Both final scenarios use Plugin Lab base `omarchy-2026.08.27-x86_64-local`, 5120 MiB of guest memory, and SSH forwarding bound to localhost. Evidence is retained under the lab base's `runs/<run-id>` directory, outside Git. Every important interaction pairs QMP keyboard or pointer input with a bounded state assertion; screenshots supplement those assertions.

The focused command was `OMARCHY_LAB_SOURCE=<clean-detached-checkout> ./bin/lab plugin <radar-checkout>/tests/lab/briefing.sh`. Run `20260905-212255` passed with 20 captures against a clean detached Omarchy checkout at `83881e979b35468c3e7d60b171e319ede61a88fd`. The scenario records the exact Radar source commit and empty source status, asserts the loaded runtime identity, and covers:

- Default Browse choice without implicit reads, saved-story retention, exact three-member group reading, and source-history keyboard navigation to the selected original URL.
- A completed brief remaining complete through refresh and reopen, with explicit New briefing selecting another unread discovery.
- Narrow dark/light rendering and expanded grouped updates.
- Both welcome choices, all six section destinations, and the real New briefing control at 200-percent text. The window is normalized wholly inside the monitor before geometry assertions. The final enlarged headline is fully visible; longer row content remains scrollable.
- Start from today reading only the displayed edition, preserving the saved story and baseline, followed by a newly received backdated occurrence staying unread and becoming the next briefing.
- Close/removal stopping owned helpers, unloading the plugin, retaining reader state, and leaving clean runtime diagnostics.

The full product command was `./bin/lab plugin <radar-checkout>/tests/lab/acceptance.sh`. Final run `20260905-212542` passed with 50 captures against the same clean Radar runtime candidate. The guest reran all 201 tests and its available validation checks; ShellCheck and QML lint were unavailable there and are covered by the host validation above. Machine assertions covered the Apps launcher, exact window identity, summon/focus and Alt+Tab, passive unread discovery, newspaper pointer actions and zero-gap hiding, shortcut installation and removal, cached-first reading, filters and saved state, source links and image filtering, malformed/oversized/offline/partial/empty editions, long Unicode content, dense keyboard pagination, same-path runtime replacement, disable/re-enable, and clean removal. Close and removal left no owned helper process.

The full scenario uses the configured Omarchy working tree at the same base commit, including its pre-existing runtime-snapshot and lock-screen edits. Its retained `git diff HEAD --binary` has SHA-256 `733d226c66691d7258dec2387487e5563a27567feb348085e9c87f85f7d71652`; the pre-existing untracked `LockKeyboard.qml` has SHA-256 `2743cead7f25a1e71990bb80cea31698262e9ac4049c783c766246f58186dd58`. Those edits were preserved. Same-path update evidence from that scenario applies to this modified loader; the separate clean-checkout briefing run establishes the candidate's ordinary installed behavior without relying on those edits. Neither run establishes compatibility with every Omarchy version or physical machine.

An earlier attempt, `20260905-211413`, stalled during guest boot before source synchronization or plugin installation. Its boot capture was retained, its VM was stopped, and the harness discarded the overlay. It is not counted as product acceptance. Earlier successful development runs remain available, but the final candidate claims above use the final run identities.

## Review and release boundary

Visual review of the final focused run covered the finite brief, expanded group in light mode, and the full enlarged headline at 200-percent text. The section rail scrolls at that scale. This is targeted visual and keyboard evidence, not an accessibility certification or a claim that every long article fits without scrolling.

The active desktop, installed plugin, homepage checkout, and Forge deployment were not changed. Both final guest overlays were discarded after stopping, and screenshots, state dumps, and logs remain outside Git. No push, tag, release, or marketplace submission was made. Production bandwidth savings have not been measured after deployment; the gzip claim is protocol and failure-path coverage.

The retained public-install scenario still targets released `0.4.16` and state v11. Before publication, update that scenario to the reviewed candidate, verify the exact public commit and first-use journey, refresh release media, and complete the owner-authorized Forge and marketplace gates in the [release contract](../RELEASE.md). Impact/version intelligence, contributor intake, and shareable historical editions are later product work, not features claimed by this candidate.
