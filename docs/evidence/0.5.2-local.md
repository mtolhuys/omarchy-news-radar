# 0.5.2 verification evidence

The owner tested the installed candidate and approved publication on 2026-09-06. Product source is commit `73fc2f42d7031da7d0c09a5aef92d2e8f4ffa4d5`; release preparation changes documentation and the public-install version assertion only. Panel identity is `news-radar-0.5.2+identity-2`, helper identity is `news-radar-0.5.2`, and state/feed schemas remain unchanged.

## Source and local acceptance

- All 313 offline source tests passed.
- Repository validation passed, including ShellCheck and QML validation against the selected Omarchy checkout.
- The focused disposable Plugin Lab briefing scenario passed in run `20260906-184421`, with a clean source checkout. Its stopped overlay was discarded and 23 screenshots retained.
- An earlier run stopped on an outdated test expectation for the already-renamed Load new briefing button. The corrected assertion passed without changing product behavior.

The passing journey covered first use, saves, scoped group reading, keyboard focus, durable completion, refresh/reopen, late arrivals, maintained light/dark layouts, 200 percent text, and removal. The regression seeded a legacy marketplace-wide follow: For You opened directly into three installed-plugin stories, while Plugins contained seven. Switching All to Unread only reduced the visible list to one, explained six hidden read stories, and kept the actual rendered unread badge at one. My setup and its news footer remained keyboard reachable. Settings was clickable inside the resized 1080-by-760 reader.

Visual inspection of the retained Plugins screenshots confirmed a readable section heading, visible actions, consistent unread badges and wrapped filter feedback. The installed local runtime reported the new build identity; read state and saves were preserved with a rollback reference.

## Publication boundary

The release commit must pass GitHub CI and the exact GitHub-clone install/read/remove scenario before tagging. This document does not claim marketplace approval: the existing request needs fresh validation for the release SHA and maintainer review of the unchanged optional setup capability. No permissions, dependencies, endpoints or executable installation hooks are added by this patch. The previous release's broader lifecycle evidence remains historical evidence, not a substitute for the focused checks above.
