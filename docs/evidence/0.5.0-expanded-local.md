# Expanded 0.5.0 local verification

This record concerns the expanded D058–D060 candidate, not the earlier briefing-only implementation. Public Radar remains 0.4.16. No public push, tag, marketplace promotion or Forge deployment is established by this evidence.

## Source and server

The final runtime candidate is `d951187061cc0b8b53c4e28b9c5a678e444dfad1`. Its source gates passed on 6 September 2026 (Europe/Amsterdam): 310 unit/integration tests, Python compilation, schema/fixture/manifest/workflow validation, ShellCheck for fifteen scripts, desktop-file validation, and QML lint against the selected current Omarchy source. Offline fixture and static-site generation also passed. The network integration cases use only a localhost fixture server; the default sandbox denied socket creation, so the complete suite was rerun with loopback permission and passed.

The homepage feature branch contains the tested server changes in commits `5cdf77b35dbaa501d3bdad492860f5c249e37680`, `3e6a988` and `cb59cca`. Its tree matches isolated server commit `81655ceeffb5bf3ad3ab0d7b0b7c9d3a2372c8af`. The final isolated full suite completed at 20:29:49 UTC with 120 tests and 1,009 assertions, no failures or skips. Pint and the unchanged Vite frontend build passed. Two pre-existing PHP 8.5 PDO constant deprecations remain. Tests used an in-memory database and did not change the homepage's real database or environment files.

Server tests execute the actual Python publisher and serve every generated artifact. A second generation removes a story and workflow and advances the week; previously shared URLs remain available. Recovery covers interruption around promotion, a partly deleted committed backup, and unsafe recovery material. Directory aliases redirect to trailing-slash URLs so browsers resolve nested styles and navigation correctly. HTTP coverage includes encoding quality values, identity/gzip validators, conditional precedence, HEAD/304, private representation-cache bounds and encoded response-byte accounting.

The measured real feed was 1,083,465 identity bytes and 112,543 gzip bytes. One hundred bodyless conditional gzip responses used approximately 1.4147 seconds of CPU before representation caching and 0.1322 seconds after. This measures the isolated response layer, not production requests per second or total Forge capacity. Proxy behavior still needs verification when deployment is authorized.

## Public browser review

The public home, a real long-title article and a workflow page were inspected in the browser. The current layout presents seven headline teasers before discoveries; article markers become validated HTTPS links and irrelevant marketplace metadata is omitted. At a 390-pixel viewport, the document was 375 pixels wide with no horizontal overflow. Desktop and narrow layouts were reviewed; no screen-reader certification is claimed. The temporary localhost preview and its browser tab were closed after review.

The real preview companion was rebuilt at 20:31:47 UTC with 100 projects and three reviewed workflow ideas. It contains 30 Omarchy, ten Radar and four Pomodoro published-release records; the other reviewed repositories can legitimately have no GitHub releases. Its optional curated images passed validation. These are public source facts, not an imported user's setup or reading history. Synthetic workflow and plugin fixtures remain confined to tests.

## Independent review corrections

Review found and corrected loss of retained rich notes during optional fetch failure, code examples being extracted as changes, ambiguous historical repository attribution, missing workflow primary links, detail lookup disappearing under search/mute, misleading local-candidate update warnings, and built-in shell components appearing as unidentified plugins. Source tests cover these boundaries.

Optional image requests are deduplicated and limited to four concurrent inspections with a 60-second queue budget; queued previews are omitted when the budget expires. Each running request still has its ordinary timeout. Body retrieval uses available-byte reads so a slow body cannot hide the deadline while filling a large buffer; virtual-clock tests cover identity, gzip and late EOF. Python's internal header/chunk framing still uses socket inactivity timeouts, so this is not a universal hard wall-clock deadline across every protocol phase.

## Desktop acceptance

Both final disposable desktop journeys passed on the exact clean runtime candidate above, using the selected Omarchy development source with core correction `e5b0dc2257530508c020f1d7f0111331cdbfb625`. Their harnesses exited successfully and their disposable VMs were stopped. Run identifiers below refer to retained external Plugin Lab evidence, not files committed to this repository.

Opening run `20260906-001524` retained 19 screenshots. Three ordinary terminal application windows stayed at their exact captured addresses and rectangles across first mapping, repeated bar and shortcut summons, close/reopen and saved placement. All 13 mapped samples in the initial 24-sample trace showed Radar already floating at 1120 × 720, at (80, 53), inside the usable monitor area. Actual compositor maximize and restore passed; restore returned to the exact previous rectangle without disturbing the other applications. A moved 900 × 660 window reopened at its saved placement. Repeated summon preserved the focused reading control.

The same opening journey reviewed real public content: 500 events, 100 projects and three reviewed workflows; light/dark Home, My setup, collection and project details, keyboard order and scroll return. Empty Saved, search and feed views omitted the inspector and story actions, offered a useful recovery route, and left reading state unchanged under story shortcuts. The initial guest boot stalled before candidate code ran; one guest-only reset recovered it, and the failed-boot capture was retained.

Maintained acceptance run `20260906-002156` retained 52 screenshots and passed all 310 source tests inside the guest. It covered Apps-menu launch, onboarding Browse, closed-panel badge updates, cached/offline reading, stale-feed recovery, exact per-story read/save behavior, native window controls, dense keyboard navigation, long text, themes, monitor changes, reduced motion, runtime replacement, disable/re-enable, launcher removal and plugin removal with state preservation. The final journal gate passed.

At 200% text, the moved window refitted within the reserved workarea, and the compact reader displayed the complete article. Real Page Down input reached the final paragraph beyond the teaser; a pointer click on the rendered RichText link opened the exact expected HTTPS URL once through the inert test shim. Page Up/Down preserved selection, read/save decisions and briefing membership. Original-source keyboard opening and a fresh enlarged reopen also passed. Heading and final-paragraph screenshots were visually inspected.

Dedicated briefing run `20260905-232348` passed on earlier source `4a6fe6952fdff8dbe8821d2d80692d7c0772b604`. This is carried-forward evidence, not a claim that it reran on the final candidate. Briefing, onboarding and group-read logic and controls were unchanged afterward; it covers both welcome decisions, exact group-read scope, explicit completion, saved-state preservation and older arrivals. The final opening and maintained acceptance journeys separately cover the subsequent window lifecycle and compact-body changes.

## Corrections established by runtime review

The decomposition exposed a duplicate QML search signal missed by ordinary lint; it was fixed and a declaration-collision gate added. Enlarged-text inspection then exposed overflowing window geometry and fixed compact controls consuming the reading viewport. Live fitting and scrollable compact controls now have real geometry and reading assertions. Both reader layouts share the complete escaped article renderer.

Opening trace `20260906-000357` established that the selected development core repeatedly rewrote a watched display-state file after every configuration reload. The resulting reload loop erased temporary placement rules between preparation and mapping. Core correction `e5b0dc22` skips unchanged writes; its isolated regression failed before the correction and passed afterward. The installed host did not contain that newer watcher behavior, so no host core deployment was performed.

Radar also retains acknowledged opening-rule specifications for reuse and allows superseded token-guarded timers to expire harmlessly. Actual Lua regressions cover stale cleanup/expiry, repeated opens, rejected-declaration retries and late confirmation. Native maximize now uses the supported dispatcher and actual compositor state, rather than Qt's decoration-related maximized hint; the final VM runs prove the real mode and restored geometry. Inline body links remain pointer-operated, with a separate keyboard-accessible original-source action. No screen-reader certification is claimed.

## Authorized daily candidate

The exact tested runtime candidate was installed into the owner's existing daily checkout after both final journeys passed. The guarded installer created a private backup, quiesced only Radar, migrated state v12 to v13, and confirmed preservation of all 478 existing read decisions, the saved-story set, public Git origin and shell/bar configuration. The installed checkout remained clean. No private state, backup contents or machine-local backup paths are committed here.

The validated public-source insights preview was preloaded for hands-on testing; its SHA-256 is `268c3b718d2f1f89ec9a70310c3cbc695fc00c7f971ca70cffece18d692bbe47`. This is cached candidate coverage, not evidence of a live companion-feed deployment. The server changes remain local, public Radar remains 0.4.16, and public push, release, marketplace promotion and Forge deployment remain outstanding owner-controlled publication steps. Documentation commits after the runtime SHA do not change the tested or installed code.
