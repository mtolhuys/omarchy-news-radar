# 0.5.5 local candidate evidence

Status: source, disposable-desktop, released-upgrade, owner daily-desktop, remote CI, and exact public-clone gates passed. The owner approved publication on 2026-09-07. Tag, release, and the in-place marketplace verification update remain publication gates.

## Regression coverage

- `make test` passed all 329 tests. The QML contract checks the exact active-window event, confirmed-focus gate, focus-yield trace, and normal close request.
- `OMARCHY_SOURCE=<current-omarchy-checkout> make validate` passed Python compilation, repository and schema checks, ShellCheck for 15 scripts, desktop-file validation, and every QML file through `qmllint` against the current Omarchy checkout.
- The lifecycle arms focus transfer only after the exact `org.quickshell` / `📰 Omarchy News Radar` identity gains focus. A later active-window transition dismisses Radar through the existing bounded close path; opening state cannot be lost to mapping-time events.
- The acceptance harness now waits for the compositor client to disappear before reopening. This removes a prior test race where a shortcut could legitimately refocus the still-closing instance while its durable unread state remained correct.

## Disposable Omarchy acceptance

Full Plugin Lab run `20260907-173935` passed from a disposable base-image overlay. The guest reran all 329 source tests and retained 53 screenshots. Machine assertions covered Apps launch, first use, cached/offline operation, bar placement and hiding, shortcut setup, update checking, move/resize/maximize, exact AltTab/Omadock identity, real competing-window focus, Alt+Tab-away dismissal, newspaper and shortcut reopening, rapid foreground activation, keyboard navigation, filters, setup relevance, source opening, background edition adoption, explicit per-story and batch reading state, malformed/oversized/partial/offline/empty/dense/long editions, 200-percent text, monitor changes, hot update, disable/re-enable, launcher removal, plugin removal, and owned-helper teardown.

The competing-window matrix proved that a newly launched terminal receives focus and Radar disappears; a real newspaper click reopens exactly one Radar window; direct terminal focus dismisses it again; `Super+Alt+N` reopens it; foreground and rapid repeated activation retain one focused instance; and Alt+Tab away dismisses Radar before the shortcut reopens it.

Released-upgrade run `20260907-174654` first reproduced the 0.1.3 background toggle defect through the actual newspaper and shortcut. It then updated through the official plugin path, migrated only the exact owned legacy action, proved candidate focus-yield dismissal, and reopened one focused candidate through both the live shortcut and newspaper. It retained five screenshots and reported no Hyprland configuration error.

The full and upgrade contact sheets were visually reviewed. Radar remained bounded, legible, and unclipped across the maintained dark/light, wide/narrow, long-content, and enlarged-text checkpoints. Focus-transfer frames left the selected application unobscured.

## Owner acceptance

The owner tested runtime commit `968cdf5` on the daily desktop and reported the focus behavior as good. The tested runtime dismisses when another application wins focus and reopens through the existing public activation routes. The subsequent release preparation changes only version identity, release documentation, banner copy, and the public-install version assertion.

## Public candidate

Release-preparation commit `7c5eafa36740301d54984f46e5ad11f3665a7c00` passed GitHub Actions run `34140894427`. Public-clone Plugin Lab run `20260907-175826` then cloned that exact SHA from `https://github.com/mtolhuys/omarchy-news-radar` and passed the documented standard installation, launcher, welcome, Front Page, per-story read/unread, shortcut, close, launcher cleanup, and plugin-removal journey. The run retained nine screenshots, which were visually reviewed without clipping, stale identity, or unexpected foreground residue.

## Remaining publication gates

- Push this documentation-only evidence commit and require green GitHub CI.
- Create tag and release `v0.5.5`, then edit marketplace issue #4892 once to the same final SHA. Do not create another issue or progress comment.
