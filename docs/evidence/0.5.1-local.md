# Version 0.5.1 local-candidate evidence

## Identity and scope

- Runtime candidate: `f50aba4b11dfbc1f9fdba47ac3d9a94baf4a90f9` on `fix/setup-inventory`. The following evidence/media commit changes only README, release evidence, and the two preview assets.
- Manifest/helper version: `0.5.1`; panel identity: `news-radar-0.5.1+identity-2`.
- Feed schema: v2; insights schema: v1; private state schema: v13. These contracts and the plugin/window identity are unchanged from 0.5.0.
- At the time of this record the public `main` and `v0.5.0` tag still resolve to `2388572`; no push, tag, release, marketplace update, or Forge deployment has occurred.

The candidate completes keyboard access in discovery, detail, Tune, and Settings; makes My setup useful with local manifest facts when public release coverage is absent; explains mute-emptied sections; and retains a completed Front Page until the reader explicitly loads an available replacement.

## Source verification

- `make test`: all 311 offline unit and integration tests passed, both on the host and again inside the disposable guest.
- `OMARCHY_SOURCE=<configured-omarchy-checkout> make validate`: Python compilation, strict JSON/schema/manifest/workflow checks, ShellCheck for 15 scripts, desktop-file validation, and QML lint passed.
- `shellcheck tests/lab/acceptance.sh` and `git diff --check` passed after the final collection-navigation assertions were added.
- Regression coverage proves completed briefing IDs remain stable and read, a newly eligible briefing is announced without replacement, mute-emptied sections report the exact blocking scope/count, and the new keyboard routes remain declared by QML.

## Daily-desktop review

The owner-authorized local installation loaded `news-radar-0.5.1+identity-2` while retaining its public GitHub origin and private reading state. The real cached edition contained 499 events, including 462 plugin events. A pre-existing local `marketplace` source mute explained the apparently empty Plugins section; clearing that mute restored all 462 matching stories without changing the feed. The live Front Page then retained its completed one-item edition with `remaining: 0`, while **Check for new stories** remained available.

## Disposable Omarchy acceptance

The complete host-driven journey passed in run `20260906-131553`:

`<plugin-lab-run-root>/omarchy-2026.08.27-x86_64-local/runs/20260906-131553`

The run used Omarchy base revision `83881e979b35468c3e7d60b171e319ede61a88fd` and base-image SHA-256 `5829324fb5cd1baa81a66c4aa9f1cb12023f53cd5918252bda8628fdd0afdaa6`. The configured Omarchy checkout's already-present tracked diff (`733d226c...`) and untracked `LockKeyboard.qml` (`2743cead...`) were preserved; the disposable overlay was discarded.

Machine assertions covered Apps launch, bar activation, global shortcut, first-use choices, cached/offline operation, update progress, source freshness, normal move/resize/maximize, Alt+Tab, focus with another ordinary window open, repeated summons, search, save, source opening, filters, 200% text, reduced motion, same-path update, disable/re-enable, removal, and owned-file cleanup. They also proved:

- `t` opens Tune, `,` opens section Settings, arrows move spatially, and Home/End reach their boundary controls.
- Front Page arrows reach a reviewed collection; Enter opens it; arrows move between included project cards; Enter opens a project; Escape returns through both levels.
- A completed briefing remains a visible read edition, announces a replacement only when eligible unread news exists, and changes only after **Load new briefing**.
- Empty, invalid, oversized, partial, offline, long-body, dense, light, dark, narrow, and large-text states remain recoverable.
- Several-window behavior raises the existing Radar instance, preserves one toplevel, and returns focus through Alt+Tab without disturbing neighboring windows.

The run retained 53 screenshots. Their contact-sheet review found no clipped required control, empty inspector chrome, unreadable focus state, or material overlap in the maintained layouts. Retained logs contain no QML `ReferenceError`, `TypeError`, load failure, crash, or Hyprland configuration error.

## Release media

- README banner SHA-256: `0d76941d44838a880e7ffc7a490ee739c11c867cc0b808606a75fa4e2781c5fd`.
- Marketplace `preview.png` SHA-256: `b7ee50d8310b96a914b307d33dee22d07f47eadb0da26a8b3cd6ca9855bc8cc2`.
- The marketplace preview is cropped from the exact passing VM's `success-news-radar-preview-matte-black.png` and visibly shows the retained Front Page plus **A new briefing is ready** / **Load new briefing** state.

## Remaining publication gates

The public clean-clone scenario cannot target 0.5.1 until the reviewed commit exists remotely. Publication therefore still requires the ordered gates in `docs/RELEASE.md`: push the reviewed candidate, fast-forward the Forge source checkout and confirm a successful scheduled publication, run the exact remote public-install journey, create `v0.5.1`, then update the existing marketplace verification request to the final full SHA. The current marketplace snapshot remains live until its maintainers complete their review.
