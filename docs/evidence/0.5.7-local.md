# 0.5.7 local candidate evidence

Status: Saved-bookmark durability fix merged to main; release identity prepared for tag, GitHub release, and marketplace verification. Broad disposable-desktop and public-clone journeys from 0.5.6 remain the Omarchy 4.0.3 compatibility baseline.

## Regression coverage

- Unit suite `tests.unit.test_saved_persistence` proves:
  - a persisted save absent from the live feed still projects into Saved;
  - live feed copies are preferred when present;
  - restored rows carry `isArchivedSave` and remain valid event shapes for the reader.
- `python -m unittest tests.unit.test_saved_persistence -v` passed on the fix branch before merge.
- GitHub Actions for pull request #17 passed on the fix commit and the merge commit on `main`.

## Product behavior

- Saved count and rail membership follow persisted `state.json` saves, not feed retention alone.
- Restored bookmarks keep title and original `sourceUrl`; summary explains they were restored from the local saved copy.
- No Panel layout change beyond the release identity string.

## Safety and compatibility

- No feed or state schema change.
- No new permissions, dependencies, network origins, installer behavior, daemons, or user-configuration mutation.
- For You continues to reuse the identity filter path without synthesizing archived saves.

## Remaining publication gates

- Green GitHub CI on the release-preparation commit.
- Publish tag and release `v0.5.7`.
- Open marketplace verification for `io.github.mtolhuys.news-radar` 0.5.7 at the exact release SHA.
